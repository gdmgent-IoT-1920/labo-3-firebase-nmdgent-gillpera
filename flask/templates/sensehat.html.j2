<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>IoT | Labo 3 | Real Time Firebase</title>
	<style>
	#senseEmulator
	{
		width: 300px;
		height: 300px;
		background-color: black;
	}
	</style>
</head>
<body>
	{% if sense_values %}
		<h2>Change the color of the SenseHat</h2>
		<div id="senseEmulator" style="background-color: {{ sense_values['value'] }};"></div>
		<form action="sensehat" method="POST">
			<input type="color" name="senseColor" id="senseColor" value="{{ sense_values['value'] }}">
			<button type="submit">Change Color</button>
		</form>

		<div class="sensor-data">
			<p id="temp"></p>
			<p id="humi"></p>
		</div>

	{% else %}
		<p>Geen data</p>
	{% endif %}
</body>
</html>